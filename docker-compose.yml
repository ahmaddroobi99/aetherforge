services:
  api:
    image: python:3.12-slim
    working_dir: /app
    volumes:
      - ./:/app
    command: >
      bash -lc "pip install -e . && python -m aetherforge.cli serve --host 0.0.0.0 --port 8080"
    ports:
      - "8080:8080"
